id: ESE2030-0337
legacy_id: null
week: 4
skills:
- W04.S08
status: active
problem_type: null
statement: |
  Let $T : V \to V$ be a linear operator on a finite-dimensional space and $\mathbf{v} \in V$. Consider the following quantities:
  
  I. $\dim \ker T$
  
  II. The coordinate vector $[\mathbf{v}]_B$
  
  III. The trace of the matrix representing $T$
  
  Which of them require a choice of basis before they can be computed?
choices:
- label: A
  text: I only
- label: B
  text: II only
- label: C
  text: III only
- label: D
  text: II and III only
- label: E
  text: I and III only
figures: []
answer:
  correct: B
  partial_credit: []
  verified: true
  verified_from:
  - prof-g review 2026-09-10
  conflict: null
  explanation: |
    Coordinates exist only relative to a basis: the same vector has different coordinate vectors in different bases, so II requires the choice. The kernel of $T$ is defined by the map alone, with no basis in sight, so its dimension is coordinate-free. The trace is subtler: one computes it from a matrix, which needs a basis, but similar matrices have equal traces, so the number obtained does not depend on which basis was used. It is a property of the operator that happens to be computed through coordinates.
  key_insight: null
  distractor_rationale:
    A: |
      Distractor: the kernel is $\{\mathbf{v} : T\mathbf{v} = \mathbf{0}\}$, a set defined by $T$ alone; no basis enters.
    C: |
      Distractor: the trace is basis-independent (a similarity invariant), and coordinates are the one thing here that is not.
    D: |
      Trick: the trace is computed via a matrix, but every basis gives the same answer, so nothing is being chosen. A quantity that needs a basis to compute but not to define is coordinate-free.
    E: |
      Distractor: both of these are properties of $T$ itself; neither depends on a basis.
sources:
- semester: POOL
  instrument: pool
  position: 45
  file: generated by Claude 2026-09-10 against WEEK 4 v2 ESSENTIAL SKILLS (MC-skill-2030 protocol)
variant_of: null
topics: []
notes: |
  GENERATED gaps pool slot 11 | FORM = CLS (subset) | SOURCE = WEEK_4 skill S08 (which questions require a basis: dimension and quotient do not; coordinates and matrices do); CORE 5 similarity invariants (trace) | difficulty: Deeper | approved by prof-g 2026-09-10
