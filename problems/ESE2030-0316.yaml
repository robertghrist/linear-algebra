id: ESE2030-0316
legacy_id: null
week: 13
skills:
- W13.S07
status: active
problem_type: null
statement: |
  Take the degenerate case of a network with no hidden layer, $\hat{y} = \mathbf{w}^T\mathbf{x} + b$, trained by gradient descent on the squared loss over a dataset. Let $A$ be the matrix whose rows are the input examples (each augmented by a $1$ for the bias), and let $\mathbf{r}$ be the vector of residuals, prediction minus target, stacked over the data. Gradient descent has reached a fixed point exactly when $\mathbf{r}$ lies in which space?
choices:
- label: A
  text: $(\operatorname{im} A)^\perp$
- label: B
  text: $\operatorname{im} A$
- label: C
  text: $\ker A$
- label: D
  text: $(\ker A)^\perp$
- label: E
  text: $\{\mathbf{0}\}$ only
figures: []
answer:
  correct: A
  partial_credit: []
  verified: true
  verified_from:
  - prof-g review 2026-09-10
  conflict: null
  explanation: |
    With no hidden layer the loss is $\tfrac12\|A\boldsymbol{\psi} - \mathbf{y}\|^2$ in the parameters $\boldsymbol{\psi} = (\mathbf{w}, b)$: ordinary least squares. The output error signal is the residual $\mathbf{r} = A\boldsymbol{\psi} - \mathbf{y}$ (transposed into a row), and the gradient with respect to the parameters is $A^T\mathbf{r}$. Descent stops exactly when $A^T\mathbf{r} = \mathbf{0}$, i.e. $\mathbf{r} \in \ker(A^T) = (\operatorname{im} A)^\perp$: the residual is orthogonal to the image, which is the normal equations of Chapter 6 and the cokernel of the Fundamental Theorem. Training the last layer of any network is this same problem in the features $\mathbf{h}_{\Lambda-1}$.
  key_insight: null
  distractor_rationale:
    B: |
      Trick: the residual lies in the image only when it can still be reduced. At the optimum the residual is what the image cannot reach --- it is orthogonal to the image, not inside it.
    C: |
      Distractor: kernel of $A$ lives on the domain (parameter) side; $\mathbf{r}$ is a vector of outputs, one per example. The right kernel is $\ker(A^T)$.
    D: |
      Distractor: the coimage, again a domain-side space. Confuses the residual with the parameters.
    E: |
      Trick: descent stops when the gradient vanishes, not when the fit is perfect. A perfect fit is one fixed point; any residual orthogonal to the image is another, and it is the generic one.
sources:
- semester: POOL
  instrument: pool
  position: 24
  file: generated by Claude 2026-09-10 against WEEK 13 v2 ESSENTIAL SKILLS (MC-skill-2030 protocol)
variant_of: null
topics: []
notes: 'GENERATED week-13 pool slot 4 | FORM = BND | WEEK = 13, 6 SYNTHESIS | SOURCE = WEEK_13 CORE 3 (base
  case) + CONNECTIONS "Week 6 least squares residual is the output error signal"; WEEK_6 coker = (im
  A)^perp; FTLA touch | difficulty: Synthesis | approved by prof-g 2026-09-10'
